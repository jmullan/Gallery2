{* 
 * biggerFontSize.tpl - display the content in a big font.  Smaller than
 *                     "biggestFont", but not as small as "bigFont"
 * 
 * Input variables:
 *
 *  $content (string)
 *     Any HTML content
 * 
 *}
<font size="+2">{$content}</font>
