{* 
 * smallFontSize.tpl - display the content in a small font.  Bigger than
 *                     "smallerFont", but not as big as normal.
 * 
 * Input variables:
 *
 *  $content (string)
 *     Any HTML content
 * 
 *}
<font size="-1">{$content}</font>
