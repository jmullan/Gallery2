{* 
 * bigFontSize.tpl - display the content in a big font.  Bigger than
 *                   normal, but smaller than "biggerFont"
 * 
 * Input variables:
 *
 *  $content (string)
 *     Any HTML content
 * 
 *}
<font size="+1">{$content}</font>
