{g->main}
  {g->pagebox}
    {g->banner}
      {g->title}
	{g->text text="Security Violation"}
      {/g->title}
    {/g->banner}

    {g->box}
      {g->description}
	{g->text text="The action you attempted is not permitted."}
      {/g->description}
    {/g->box}
  {/g->pagebox}
{/g->main}
