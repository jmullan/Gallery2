{g->pagebox}
  {g->banner}
    {g->title}
      {g->text text="Comments Settings"}
    {/g->title}
  {/g->banner}

  {g->box style="admin"}
    {g->element}
      {g->text text="No settings available for this module."} 
    {/g->element}
  {/g->box}
{/g->pagebox}
