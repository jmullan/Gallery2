{g->pagebox}
  {g->banner}
    {g->title}
      {g->text text="Search Settings"}
    {/g->title}
  {/g->banner}

  {g->box style="admin"}
    {g->element}
      {g->text text="No available settings for the Search module."}
    {/g->element}
  {/g->box}
{/g->pagebox}
